#include<iostream>
#include <vector>
using std::string;
using std::cin;
using std::cout;
using std::endl;
using std::vector;

int largerInt(int a, int *pb){
  return a > *pb ? a : *pb;
}

int main(int argc, char const *argv[]) {

  return 0;
}
